# Demo mode is enabled by default and works without backend or internet.
# Set to true only when the Django API is running and should be used.

# Remote font catalog is disabled for a reliable offline presentation.
VITE_ENABLE_REMOTE_FONTS=false

VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1
